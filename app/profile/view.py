from flask import render_template

from .points import get_total_point


'''
@profile.route('/<int:user_id>')
def profile(user_id):
    points = get_total_point(user_id)
    return render_template()
'''